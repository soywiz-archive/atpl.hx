package lang;

interface ITemplateParser {
    function addBlockFlowExceptionHandler(name: String);
    //function addBlockHandler(name: String, callback: (blockType, templateParser, tokenParserContext, templateTokenReader, expressionTokenReader) => void);
}
